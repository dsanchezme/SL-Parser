programa declaraciones_variables_const_4

var
  n, suma, k : numerico
  v_disp, // Velocidad inicial
  a_disp : numerico // Angulo de disparo
  a_disp_rad : numerico; // Igual que a_disp pero en radianes
  h_max : numerico // Altura maxima

const
  inicio_tabla = 32;
  fin_tabla = 255
  INCR = 16
  RELLENO = ' '
  k, tope : numerico  
  RAIZ_3 = 1.732050808
  
inicio
  imprimir ("Hola, aquí solo se hacen declaraciones de variables y constantes")
fin